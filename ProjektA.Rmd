---
title: "Projekt A Övervakad statistisk inlärning"
author: "Christoffer Macias"
date: "`r Sys.Date()`"
output:
  pdf_document: default
  html_document:
    df_print: paged
---

```{r}

```


```{r}
c <- read.csv("A2_charter.csv")

tommodel <- lm(y~1, c)
summary(tommodel)
enkelmodel <- lm(y~x1+x2+x3+x4+x5+x6+x7+x8+x9+x10+x11+x12+x13+x14+x15+x16+x17+x18+x19, c)
summary(enkelmodel)

korr_matrix<-cor(c)
korr_matrix<-korr_matrix[,-1]
korr_matrix<-korr_matrix[-1,]
korr_matrix[upper.tri(korr_matrix, diag = TRUE)] <- 0

idx <- which(abs(korr_matrix) > 0.7, arr.ind = TRUE)

multiz <- data.frame(
  namn = paste0(rownames(korr_matrix)[idx[,1]], "-", colnames(korr_matrix)[idx[,2]]),
  observation = korr_matrix[idx]
)

print(multiz[order(-abs(multiz$observation)), ])



#pairs(~y+x1+x2+x3+x4+x5+x6+x7+x8+x9+x10+x11+x12+x13+x14+x15+x16+x17+x18+x19, data = c)
#head(c)
```
